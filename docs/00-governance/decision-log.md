# Product Decision Log

This log records explicit Joshua decisions that supersede older prose. Chat history is not an implementation contract unless its decision is captured here or in another authoritative requirement document.

| Date | Decision | Superseded requirement/source | Affected IDs | Implementation status |
| --- | --- | --- | --- | --- |
| 2026-08-03 | Routine logs, crash data, and disposable diagnostics are machine-local. User-retained reports, durable diagnostics, analysis/tuning history, learned knowledge, exports, and backup metadata are portable Documents data. | Broad `PORT-001` wording and any reading of `SET-005` that made user-retained work disposable | `PORT-001`, `PORT-006`, `SET-005`, `UN-001` to `UN-005` | Specification reconciled; lifecycle verification required for the next candidate |
| 2026-08-03 | Garage61 and AI are configured in Connections. Settings holds ordinary preferences and subordinate diagnostics. | Earlier wording that called Garage61 a Settings workflow | `G61-001`, `CON-001` to `CON-007`, `DIAG-001` | Specification reconciled; current primary navigation already has Connections |
| 2026-08-03 | External-service credentials persist only for the same Windows user/machine and never silently transfer with `Documents\iRacing Coach`. Migration restores nonsecret state and requests reconnection. | Earlier portable-key preference and ambiguous `SEC-003`/`SEC-004` wording | `SEC-002` to `SEC-006`, `PORT-002`, `SET-006` | Decision final; expanded migration, redaction, and permissions evidence required |
| 2026-08-03 | Permanent unsupported capabilities have no production shell. Contextual controls are hidden unless expected missing evidence needs a concise `Not measured` or `Insufficient evidence` state. Absence never becomes zero or neutral. | Any implementation-oriented unavailable shell used to stand in for missing capability | `PROD-017`, `CAP-001` to `CAP-009`, `EV-003` | Specification reconciled; capability-registry repair in progress |
| 2026-08-03 | The mojibake backlog item is closed as stale because no current authoritative repository file reproduces it. | Third-party critique/export rendering artifact | `GAP-010` | Closed; reopen only with exact byte evidence |
| 2026-08-03 | Live Monitor is a movable, non-native-resizable, grid-based telemetry workspace with named portable layouts and machine-local placement/physical scale. | `LM-001` through `LM-005` where weaker or conflicting | New `LMG-*` requirements, `TRAY-*`, `SET-005`, `PERF-002` | Binding product decision; implementation in progress |
| 2026-08-05 | Live Telemetry uses a fixed square-cell logical grid that fits its host; the miniature popout alone has a separate physical-scale control. All real telemetry metrics support Number, Bar, Gauge, and Chart forms. Default is the only immutable dashboard, and routine editor instructions/status chrome are removed. | Earlier combined grid/scale control, numeric-only style restrictions, and three immutable factory dashboards | `LT-014`, `LT-015`, `LMG-001`, `LMG-002`, `LMG-004`, `LMG-007`, `LMG-009`, `LMG-013` | Implemented and verified in the 0.12.0 development snapshot |
| 2026-08-05 | Live Telemetry grid tracks divide the available dashboard width and height evenly; cells may be rectangular so every layout fills its container. The main editor is the sole layout-authoring surface, while the native popout is a fitted, display-only layout selector. Live charts interpolate between real SDK samples at display refresh rate without claiming a higher telemetry sampling rate. Race Analysis trace rows are user-configurable, one-column, and capped at ten rows with up to two distinct signals per row; paired signals retain independent scales and truthful units. | The 0.12.0 square-cell grid, native popout editing controls, direct sample-to-frame rendering, and fixed Race Analysis trace stack | `LT-013` to `LT-020`, `LMG-001` to `LMG-014`, `PERF-002`, `PERF-006`, `RA-045A`, `RA-059` | Implemented in versioned 0.13.0 source; exact packaged artifact and real-session verification remain separate gates |
| 2026-08-07 | Race Telemetry uses one left context column with Track position above Laps & runs; either panel collapses independently and the remaining panel fills the column. Run counts retain textual green/caution meaning with Home-matched color cues, the map marks start/finish, and PIT-badge detail exposes only recorded or defensibly transformed O/M/I tire condition. Settings adds one portable primary-theme choice shared by the app shell and native telemetry popout without replacing semantic telemetry/status colors. | Earlier desktop three-column Race Telemetry wording and a fixed mint-only primary accent | `RA-030`, `RA-036`, `RA-040`, `RA-040A`, `SET-010`, `UI-017` | Implemented in current development source; targeted automated and direct executable verification remain separate gates |
| 2026-08-07 | Race uses one compact header row with data tabs left, centered identity/back, and phase switch right. Track/Laps headings are the collapse buttons and keep content mounted; traces support independent Clean/Green filtering and a non-caution spotlight. Compact accessible pit-direction icons supersede visible `PIT (in)` / `PIT (out)` text. Incident badges appear only for source-backed events, preserving an explicit `x0` without inventing one for an eventless lap or claiming that unobservable zero-point contacts were absent. Left tires display O/M/I and right tires I/M/O. Live and Race toolboxes share one motion/reflow token. | The second Race data-selector row in `RA-023`, visible PIT text in `RA-032`, detached collapse chevrons, and unsynchronized drawer/page transitions | `RA-020`, `RA-023`, `RA-032`, `RA-033`, `RA-036`, `RA-040A`, `RA-041`, `RA-059`, `RA-059A`, `TD-027A`, `UI-018` | Reconciled in authoritative specification and guarded by focused source-contract tests; direct visual and real-session verification remain separate gates |
| 2026-08-07 | Race Telemetry keeps the Back arrow and visible `Back` label immediately beside the event identity; fills the remaining viewport without page scroll; uses visible `Pit entry` / `Pit exit` labels; and keeps its no-selection canvas at the same size. Track and Laps become compact header bubbles that animate their mounted panels in the matching vertical or horizontal direction. The trace toolbox must not change the left context-column width. Spotlight receives stronger emphasis, fastest/sector cues, and a self-closing selector; the lap rail adds a leading Fastest action. The trace library expands by truthful, minimally categorized recorded signals only. Incident detail stays concise and names event types only when the recording explicitly supplies them. | The icon-only Back control, compact icon-only pit direction decision, heading-as-toggle panel controls, weak spotlight treatment, uncategorized 12-signal trace library, and source/provenance prose inside incident popovers | `RA-020`, `RA-032`, `RA-033`, `RA-040A`, `RA-056`, `RA-059`, `RA-059A`, `RA-059B`, `TD-027A`, `UI-018`, `GAP-016` | Binding specification for the current Race Analysis iteration; source-contract, animation, visual, and real-recording verification required |
| 2026-08-07 | Race Analysis adds three top sections: Telemetry, Technical data, and Race replay. Exact-configuration vector maps default to lap-colored Traces and use the binding metric/pit-layer semantics. Analysis trace layouts are separate global portable layouts with immutable Default. Technical data is a fixed 2x2 Pit strategy/Tire management/Fuel management/Racecraft & pace overview with full-area drill-ins. Race replay is a fixed-screen recorded-data visualization with synchronized participant map, leader rail, running-order grid, always-visible horizontal flag timeline, known-only comparisons, player telemetry, own fastest-clean overlay, and explicit-player-ABS highlighting. Finalized raw IBTs are permanently retained as verified content-addressed Documents data. A deterministic NASCAR-first local tire/capability model must keep measurements, proxy wear, predictions, confidence/bounds, and eventual Garage61 references distinct. | `Race review`, recorded-path-only maps, per-race/ad hoc trace configuration, reference-only raw-IBT retention, session-local proxy wear as the only tire estimate, and any visually complete replay/competitor fallback | `RA-020`, `RA-023`, `RA-040`, `RA-045A`, `RA-050B`, `RA-059C`, `RA-060` to `RA-060C`, `RA-085` to `RA-088`, `RA-120` to `RA-129`, `TD-007` to `TD-008`, `TD-031` to `TD-035`, `TD-050` to `TD-055`, `PORT-001`, `PORT-008` to `PORT-010`, `EV-011` to `EV-012`, `G61-007` to `G61-009`, `QA-012` to `QA-014` | Implemented in development source with focused structural, mapping, storage, and model tests. Exhaustive interaction, representative real-recording breadth, model calibration, lifecycle recovery, and authorized Garage61 evidence remain pending; this row is not product acceptance |
| 2026-08-08 | Progressive Tuning becomes a representative-recording, exact-turn, per-run-stage experiment workflow. Fixed races may supply driving evidence but garage output requires a compatible open target. Early/Middle/Late are per green tire run; turns support multi-symptom Entry/Center/Exit feedback plus a contextual whole-race note. Official turn provenance follows supported iRacing assets, an isolated coach HUD capture, official NASCAR/venue evidence, then user verification. Deterministic validation and a versioned NASCAR rules catalog produce the candidate whitelist before optional AI; AI can only rank/explain supplied IDs and deterministic fallback remains complete. Setup application stays manual, one primary change is shown, exact values require verified steps/ranges, and later compatible practice/test/qualifying/race analyses may prove the A/B result. | The version 0.11.0 global tuning form, telemetry `Load zone` dots as the primary selector, flattened symptom prose, UI-side duplicate experiment archive, unbounded setup suggestions, and subjective-only outcomes | `PT-001` to `PT-072` | Binding implementation decision; acceptance requires the Progressive Tuning overhaul matrix and a real clean O'Reilly/Xfinity open-setup A/B cycle |
| 2026-08-18 | The application MAY generate a new simulator-loadable `.sto` file. It MUST NOT overwrite or modify an existing one, and MUST NOT ship an unverified encoder in the production path. Investigation of a supported export/import mechanism is closed: iRacing will not provide one, and the stored payload is encrypted (measured: 7.929 bits/byte entropy, all 256 byte values present, no standard compression container at any offset, and two setups for the same car and track share 2.7% of their bytes). Setup output therefore terminates in garage-entry values rather than a file, and reading a setup uses the plaintext block iRacing writes into telemetry. | `ST-005`, `PT-049`, `PROD-003`, `SECURITY_AND_TRANSFER.md:42`, and the NDP:476 supported-STO-writing research backlog item | `ST-005`, `PT-049`, `PROD-003`, `SU-007`, `SU-008` | Amendment recorded. Generation is now permitted by policy and blocked by encryption, so no encoder work is scheduled; the setup workflow targets garage-entry values instead |
| 2026-08-18 | Provenance confirmed by Joshua during a rule-origin audit: the single-left-column Race Telemetry layout with the portable primary-theme choice (first 2026-08-07 row), the visible-Back and visible pit-label layout (third 2026-08-07 row), the three top Race Analysis sections (Telemetry / Technical data / Race replay), permanent content-addressed raw-IBT retention, and the entire Progressive Tuning workflow (2026-08-08 row) are his explicit decisions and remain binding. | Confirms prior rows against decision-log.md:3 | `RA-020`, `RA-030`, `RA-032`, `RA-033`, `RA-036`, `RA-040`, `RA-040A`, `RA-056`, `RA-059`, `RA-059B`, `SET-010`, `UI-017`, `UI-018`, `PORT-001`, `PORT-008` to `PORT-010`, `PT-001` to `PT-072` | Confirmed binding |
| 2026-08-18 | Where the two 2026-08-07 Race header rows conflict, the visible-label row governs: the visible "Back" label and the visible "Pit entry" / "Pit exit" labels are authoritative. Joshua does not recognize the same-day decision that replaced them with icon-only pit-direction glyphs and an icon-only Back control; that variant is demoted to agent inference. | The 2026-08-07 "compact pit-direction icons supersede visible PIT text" row | `RA-020`, `RA-032`, `RA-033` | Conflict resolved; icon-only variant demoted |
| 2026-08-18 | The four Technical Data subjects — pit strategy, tire management, fuel management, racecraft & pace — are Joshua's and remain required, but the FIXED 2x2 quadrant layout is not a requirement. He confirmed these concerns matter and that the explicit 2x2 does not; Technical Data layout may evolve and should be designed around each subject's data rather than a fixed grid. | The 2026-08-07 "fixed 2x2 ... overview with full-area drill-ins" clause | `TD-007` to `TD-008`, `TD-031` to `TD-035`, `TD-050` to `TD-055`, `RA-060` to `RA-060C` | Layout demoted; four subjects retained |
| 2026-08-18 | Correction of a misattributed decision. "Explicit-player-ABS highlighting" was recorded as a Race-replay feature; Joshua's actual intent is that brake telemetry drives a state change on the brake/trend trace — the trend section turns yellow when ABS is active. The ABS indicator belongs to the brake trace, not the replay. | The 2026-08-07 "explicit-player-ABS highlighting" replay clause | `RA-120` to `RA-129`, `TD-035` | Reattributed to a brake-trace indicator |
| 2026-08-18 | The NASCAR-first scope of the local tire/capability model is a starting point Joshua agreed to when the agent asked, not a hard constraint. The binding intent is a tire-wear model that keeps measurements, proxy wear, predictions, and confidence/bounds distinct; the model may extend beyond NASCAR content as data allows. | The 2026-08-07 "deterministic NASCAR-first local tire/capability model" clause | `TD-031` to `TD-035` | Scope softened; separation-of-evidence intent retained |
| 2026-08-18 | Progressive Tuning intent, in Joshua's words: per-turn feedback entry that is optional per turn (entered only where the user has feedback for that turn), iterating the tuning setup using AI, known and learned data and tendencies, tire wear, and whatever telemetry is available. Refines rather than replaces the 2026-08-08 Progressive Tuning decision. | Refines the 2026-08-08 Progressive Tuning row | `PT-001` to `PT-072` | Intent recorded; consistent with the PT family |
