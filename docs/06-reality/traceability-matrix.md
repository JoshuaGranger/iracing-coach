# Traceability Matrix

This matrix maps specification areas to implementation and verification anchors. A path demonstrates where evidence should be inspected; it does not by itself prove conformance.

| Specification | Main implementation anchors | Test/evidence anchors | Snapshot status |
| --- | --- | --- | --- |
| `HOME-*`, `NAV-*` | `CompanionShell.razor`, `NavRail.razor`, `HomePage.razor`, `CompanionState.QueueMissingHomeRaceAnalysis`, `WaitForHomeAnalysisWindowAsync` | `CoordinatorTests.HomeRefresh_PausesBackgroundAnalysisWhileLiveTelemetryIsConnected`, `HomeRefresh_PausesBackgroundAnalysisWhileInteractiveAnalysisIsLoading`, `FinalProductFixtureTests.cs`, fresh-development-executable walkthrough | Implemented; background queue and foreground priority gate are source/focused-test verified, private-library breadth and combined-load measurement pending |
| `RA-001` to `RA-025` | `AnalysisPage.razor`, `AnalysisWorkspacePage.razor`, `CompanionState.cs`, `DashboardMapper.cs`, `native_events.py` | `RaceAnalysisBehaviorTests.cs`, `FinalProductFixtureTests.cs`, `test_native_events.py`, overhaul acceptance matrix | Compact one-row header exists; the binding Telemetry/Technical data/Race replay section set supersedes the prior Telemetry/Race review pair and remains overhaul evidence scope |
| `RA-030` to `RA-059C` | `TelemetryWorkspace.razor`, `AnalysisTraceLayouts.cs`, `AnalysisTraceLayoutSets.cs`, `analysis-telemetry-cursor.js`, `RuntimeMapper.cs`, `race_foundations.py`, `storage.py`, portable settings, shared toolbox/command-bar CSS tokens | `RaceAnalysisBehaviorTests.RichTrackMap_DefaultsToTracesAndKeepsEveryPaletteAndLayerTruthful`, named-layout lifecycle/migration tests, `RaceFoundationMapperTests`, `test_race_foundations.py`, exact-configuration map and animation matrix | Exact-configuration layered vector binding, binding Type palettes, and separate global named Analysis layouts are implemented and focused-test covered. Exhaustive visual evidence and representative geometry breadth remain open; unavailable layers/signals must remain truthful |
| `RA-060` to `RA-060C` | `RaceTechnicalData.razor`, `TechnicalUnavailable.razor`, `RuntimeMapper.cs`, deterministic Technical insight/tire/reference contracts | `RaceAnalysisBehaviorTests.RaceTechnicalData_UsesFourFixedOverviewCardsAndTruthfulFullAreaInvestigations`, `RaceFoundationMapperTests`, all four direct drill-in rows in the overhaul matrix | The Race review replacement and four-category drill-in source are implemented with focused field-gating tests; fixed-screen exhaustive visual acceptance and representative evidence breadth remain open |
| `RA-070` to `RA-112` | integrated analysis insight rail and lap/run popovers, `analysis_engine.py`, `reporting.py`, `race_card.py`, `race_foundations.py`, `storage.py` schema-1 tire observation/model/prediction store | analysis/report/damage/race-card tests, `test_race_foundations.py` minimum-evidence/feature-match/idempotence checks, representative local observations | NASCAR-first learned predictions and strict measured/proxy/predicted separation are implemented in development source. Calibration, recovery breadth, and representative compatible history remain open; unsupported or weakly supported predictions remain unavailable/low-confidence |
| `RA-120` to `RA-129` | `race_foundations.py` replay extraction, `LiveReplayCaptureStore.cs`, `RuntimeMapper.cs`, `RaceReplayWorkspace.razor`, shared replay clock and CSS | `RaceAnalysisBehaviorTests.RaceReplay_GatesIncompleteCoverageAndUsesOnlyCanonicalKnownFieldData`, `RaceFoundationMapperTests`, `test_race_foundations.py`, every Race replay row in the overhaul acceptance matrix | Canonically gated recorded-only all-car/class visualization, leader rail, persistent running-order grid, continuous flag timeline, known-only comparison, own fastest-clean overlay, and explicit-player-ABS highlighting are implemented and focused-test covered; representative replay/load/visual acceptance remains open |
| `PLAN-*` | `PlanningPage.razor`, planning contracts/mappers, deterministic report data | `FinalProductFixtureTests.cs`, reporting/workflow tests | Implemented fixture workflow |
| `SETUP-*` | `SetupPage.razor`, `RuntimeMapper.SetupPackage`, `setup_catalog.py`, coordinator contracts | setup-catalog, package, and product-truth tests | Internal indexing and Starting Tune implemented; unused library UI removed |
| `TUNE-*` | `TuningPage.razor`, `TuningTrackSelector.razor`, `ProgressiveTuningFeedbackEditor.razor`, `ProgressiveTurnCatalog.cs`, `ProgressiveTrackProjection.cs`, `ProgressiveTuningCoordinator.cs`, `ProgressiveTuningModels.cs`, `tuning_engine.py`, `tuning_workflow.py`, `storage.py`, versioned `config/tuning-rules` catalog, and strict AI output schema | `ProgressiveTuningCoordinatorTests.cs`, `ProgressiveTuningUiTests.cs`, `test_structured_tuning.py`, legacy tuning-engine/workflow tests, and the Progressive Tuning overhaul acceptance matrix | Evidence-contract v2 is implemented in development source: exact source/target/map/setup identity, verified normalized turn bounds, representative clean-run selection, structured stage/phase feedback, atomic drafts/corrections, deterministic one-change whitelist, bounded AI membership validation, idempotent canonical experiment persistence, and compatible result linkage are present. Browser/native visual acceptance and a real clean compatible open-setup A/B cycle are explicitly unaccepted; only the verified O'Reilly/Xfinity rules catalog is currently eligible |
| `LIVE-*`, `LMG-*` | `LiveTelemetryLayoutGrid.razor`, `live-telemetry-layout.js`, `live-telemetry-tile-charts.js`, `LiveMonitorWindow.*` including `CompositionTarget.Rendering`, `LiveMonitorLayouts.cs`, `LiveTelemetryCatalog.cs`, `LiveTelemetry.cs`, `LiveTelemetryFrameCompaction.cs`, `IRacingSdkTelemetrySource.cs` | `LiveMonitorTests.cs`, replay/live service tests including synthetic 240 Hz reader coverage, fresh-development-executable popout walkthrough | Dedicated waitable-timer reader, equal-share full/popout layouts, bounded latest-value/extrema/gap-preserving renderer, and incremental display-synchronized native tile updates implemented; synthetic cadence is structural evidence only, with no real-SDK, 60 Hz, or 244 Hz acceptance claim |
| `CONN-*`, `SET-*`, `DIAG-*` | embedded `ConnectionsPage.razor`, compact `SettingsPage.razor`, `ThemeColors.cs`, `CompanionShell.razor`, `LiveMonitorWindow.*`, stores, logging | theme normalization/persistence/native-popout tests, coordinator/backend tests, and production-source checks; integrated visual review pending | Implemented inside compact Settings hierarchy with portable curated primary-theme selection shared by the shell and native popout; real external auth varies |
| `EV-*` | Python analysis/report/tuning modules, `EvidenceBadge.razor`, `RuntimeMapper`, grade audit UI | contract, engine, and product-truth tests | Five-category availability is explicit; rubric calibration still requires representative expert review |
| `TD-001` to `TD-046` | IBT reader, native events, storage/analysis engine, live source, coordinator schema-12 UI cache, `race_foundations.py` geometry/replay extraction, `LiveReplayCaptureStore.cs` | Python telemetry/event/storage tests including tire source-unit conversion and concurrent migration, exact-selector coordinator tests, incident mapper, derived-sideslip tests, raw-retention/geometry/replay focused checks, scoped real-IBT probes | Exact phase/cache identity, permanent raw retention, exact-config geometry, and replay schema paths are implemented. Participant/event/channel breadth and representative real-recording validation remain open |
| `TD-050` to `TD-055` | `race_foundations.py` portable tire observations/predictions and `storage.py` tire-model/Garage61 representative-lap schema-1 caches | `test_race_foundations.py` idempotence, domain, three-session floor, feature-aware confidence/bounds, raw durability, and external-scope tests; coordinator mapping checks | NASCAR-first model/source contract is implemented with guarded predictions; current calculated tire wear remains a separate session-local proxy. Calibration, atomic-recovery breadth, and authorized external evidence remain open |
| `PORT-*`, `SEC-*` | durable archive, portable logical settings, content-addressed raw IBT store, geometry/layout/tire/reference stores, schema-12 UI cache, schema-2 deterministic backend archive/index/cache, machine placement/credential stores | coordinator durable-manifest/cache-upgrade tests, atomic raw-copy/dedupe/original-removal focused tests, backend concurrent migration tests, monitor migration and path-security tests, privacy scan | Credential/placement split and permanent raw-copy/model/geometry/layout stores are implemented. Interrupted-copy recovery, backup/migration/uninstall breadth, storage UX, and privacy evidence remain pending |
| `ARCH-*`, `PROC-*` | app startup, coach engine, backend client, coordinator | backend/coordinator tests and release build | Implemented local runtime shape |
| `G61-*` | Garage61 client and credential UI/store plus `storage.py` scope-bounded representative-lap schema-1 cache and Technical data reference surface | Garage61 adapter tests, `test_race_foundations.py` scope-preservation check, coordinator mapping/UI source contracts, planned authorized alignment/model-separation evidence | Adapter/configuration and the guarded cache/display contract are implemented; authorized provider retrieval/alignment evidence remains conditional and cannot gate local analysis |
| `AI-*` | coach-engine orchestration and MCP contracts | engine probe, contract tests | Deterministic engine implemented; optional AI is noncritical/partial |
| `INST-*`, `UPG-*`, `UN-*` | installer/uninstaller, release/lifecycle scripts | installer-upgrade evidence | Locally verified; representative-PC breadth pending |
| `QA-*`, `REL-*` | build/verify scripts and release packet | release records, targeted iteration evidence, direct local/native validation | Gate scope follows the changed behavior; packaged releases require full applicable release evidence |
| `UI-*`, `ACC-*`, `PERF-*` | `coach.css`, `ThemeColors.cs`, Razor/WPF surfaces, full-page canvas renderer, native retained-tile renderer, `analysis-telemetry-cursor.js`, replay renderer/shared clock, Home analysis priority gate, performance probe | theme/application tests, source contracts, visual baselines, UI audit, targeted JavaScript/.NET assertions, scheduler tests, performance packet, Race Analysis overhaul acceptance matrix | New Race Analysis surfaces have focused structural evidence only. Every changed control/state still requires 1280x720 and 1920x1080 maximized/nonmaximized click-through, intermediate animation/clipping review, reduced-motion repetition, and replay load measurement |

## Maintenance rule

When a requirement changes, the author must inspect all mapped implementation and evidence anchors. When implementation changes behavior, the author must update the affected requirement or record a deliberate deviation. When a test changes without a requirement change, reviewers must confirm that the test still measures the requirement rather than merely current behavior.
