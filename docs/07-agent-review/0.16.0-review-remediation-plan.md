## THE IMPLEMENTATION PLAN — iRacing Coach 0.16.0 review remediation

### 0. Ground truth corrections before anything is scheduled

**The dossier is diagnosed against a branch that is not merged.** Every citation, plus `ANALYSIS_VIEW_FIELDS`, `contracts/analysis-view-v1.schema.json`, `companion-app/tests/javascript/`, and all three payload blocks, exist only on `claude/crew-chief-round-1` @ `83847c3` in `C:\Users\joshu\source\worktrees\iracing-coach-claude-strong`. On `main` @ `483033f`, `grep pace_attribution|strategy_planning|field_pace` over the scripts tree returns **zero hits** and the analysis-view schema does not exist. I verified the topology: `git rev-list --left-right --count main...claude/crew-chief-round-1` = `0 82`, merge-base *is* main's HEAD — the branch is a **strict fast-forward, 82 commits, zero conflicts**. Planning against `main` invalidates roughly 40% of the dossier's line references.

**Facts I confirmed directly on the owner's machine** (the dossier's P0 cost model is real, even though its code-path attribution is still inference):

| | |
|---|---|
| `ui-analysis-cache` | 29 files, **517 MB** |
| schema split | v5×14, v7×1, v11×10, v12×4 → **25 of 29 stale (86%)** |
| `analysis-cache` | 148 files, 2,310 MB |
| `telemetry-traces` | 30 files, 2,131 MB |
| **`reports`** | **1,021 files, 2,562 MB — largest directory in the data dir, unmentioned anywhere in the dossier** |
| total data dir | ≈7.5 GB |

Also verified: `UiAnalysisCacheSchemaVersion = 12`; `StyleClass` emits `"chart"` for Trend while `live-telemetry.css` targets `.style-trend` with **zero `.razor` hits** (dead selector confirmed); `analysis-performance.css:6` `max-content` template; `body { line-height: 20px }`; `--metric-font` referenced but **defined nowhere**; `MetricTile`/`StatusChip`/`GapCell`/`TimelineLane` have **zero call sites across `src` and `tests`**.

**Evidence status.** Of ~70 findings, exactly **four are CONFIRMED**: the `CapabilityContext` per-render rebuild cost; the moving-anchor chart grid; the run-header grid mismatch; and the absence of any test able to catch the blank-scalar class. **One is REFUTED** — the trace-SVG `ChartHeight` is **842, not 856** (`ChartBottom = 12 + 820 − (ChartRowHeight − ChartPlotHeight) = 818` whenever `TraceRows.Count ≤ 13`). Per the verifier's correction it is a genuine layout-fidelity defect worth its own item — an unmeasured height axis behind `preserveAspectRatio="none"` means cursor/tooltip geometry is computed against a scale it does not know about — **but it is not a cause of the jaggedness symptom and must not be filed against it.** Everything else is **UNVERIFIED**: code-reading inference, not observed behaviour, because no agent could build or run the app. Each package's first test is responsible for converting its hypothesis into fact.

**One brief correction.** There is **no `blocked_awaiting_aggregate` gate in any of the three payloads.** That constant lives in `tire_model_gate.py:65` and is referenced only by its own test — the module has no caller in the analysis engine. The UI has no such gated field to handle; the six real gates are ordinary status strings (enumerated in WP-11a).

---

### 1. Sequencing logic — where I override the review's priority order

Four dependency inversions:

1. **The envelope unlock outranks most of P0.** `pace_attribution`/`strategy_planning`/`field_pace` are computed but stripped by `build_analysis_view`'s allowlist before the coordinator ever sees them. This is the single highest-value item, it is **small** (three tuple entries + regen), and it also unblocks P2.4. It goes in Wave 2, not Wave 3.
2. **Shared primitives move from P3 to Wave 3-start.** All five named primitives are dead code while twelve bespoke label/value grids and ~thirty header classes exist. P1.2 (crew chief), P2.1 (lap rail), P2.2 (Home) and P3.1 (Settings) all need the same dense row/table/header vocabulary. Building it once first is cheaper than four screens inventing it and a fifth pass unifying them — which is exactly the pattern that produced today's thirty header classes.
3. **The `{}` glyph is not P3.** `DashboardMapper` yields `Layout == "{}"` while `RuntimeMapper` yields `"Richmond"`; `CompanionState.cs:296` matches `TuningResultRaces` on layout equality, so for any config-less track **the Progressive Tuning result set silently comes back empty.** Correctness, not cosmetics.
4. **Bump the cache schema exactly once, and only after the cache read path is fixed.** WP-6/7/8/9 all change what a cached analysis means. Today 25/29 files are already stale; a bump makes it 29/29 and forces a full library re-analysis through whatever path exists at that moment. WP-1 must be in first.

---

### 2. Work packages

#### WAVE 0 — Ground the tree, then measure

**WP-0.1 · Land the branch.** Fast-forward `claude/crew-chief-round-1` onto `main`. Effort XS, risk low. Verify: `tools/verify-repository.ps1`, `dotnet test`, re-run the four spot checks above. *Blocks everything.*

**WP-0.2 · Instrument before optimising.** Temporary stopwatches around `AnalyzeRaceAsync:900` (cache load), `McpBackendClient.CallToolAsync:82-91` split into spawn vs execution, `QueueMissingHomeRaceAnalysis:3629` (whole sweep), and `ShowFromTray` → first paint. **Rationale:** the dossier's own OPEN admits the one thing with a plausible 20-second budget — the backend `analyze_iracing_race` run — was never measured. Process tax (~0.5 s) and IO (~1.55 s) are measured; the work they wrap is not. WP-2 is the largest change in the P0 set and should not be built until this says the process tax is material. Effort S, risk low. Files: `CompanionState.cs`, `McpBackendClient.cs` (removed or diagnostics-gated before release).

#### WAVE 1 — P0 responsiveness

**WP-1 · Cache read path** *(closes P0 #1: tray restore + Tuning→Analysis freeze)*
Sidecar `<key>.overview.json` (RaceOverview projection + schemaVersion + sourceLastWriteUtc) written by `SaveUiAnalysisCache`, read first by `TryReadUiAnalysisCache`; schema stamp checked **before** materialising the document; `WriteIndented=false` for machine-read artifacts; `TryLoadUiAnalysisCache` → `…Async` inside `Task.Run`, awaited at `:900` and `:1260` with a yield so the spinner commits; sweep queued off the Dispatcher in `SetPrimaryUiVisible`; stale-schema files pruned under the existing archive-root containment check at `:3350`, plus an age/size cap. Also closes the double-read the diagnosis missed (`TryLoadArchiveOnlyAnalysis` → a *second* full parse on the same click).
Files: `CompanionState.cs` (single-writer), `MainWindow.xaml.cs`. Depends WP-0.1. **Effort M, risk medium** — the epoch guard (`AnalysisRequestStillCurrent`) and publishing on the caller's context are the two ways to break this. Verify: new CoordinatorTests — sidecar round-trip; stale file deleted not re-read; no off-context `CurrentAnalysis` write; a late load cannot overwrite a newer selection. Plus WP-0.2 before/after numbers.

**WP-2 · Persistent MCP session** *(P0 #1 fixed per-call tax)*
One long-lived session per `BackendConfiguration`; initialise once; multiplex `tools/call` over the existing id-correlating loop; supervisor restart on unexpected exit; per-call timeout that does not kill the session; make correlation concurrency-safe. Drop the version-probe subprocess in `start-mcp.ps1:40-49`; assert `sys.version_info` at import in `mcp_server.py`.
Files: `McpBackendClient.cs`, `start-mcp.ps1`, `mcp_server.py`. **Effort L, risk HIGH** — largest change in the P0 set; it changes the crash/restart story. This is the one place where "measure first, maybe defer" is correct. Verify: concurrent calls over one session; mid-call child death → supervised restart; timeout without teardown; `tools/mcp_e2e_smoke.py`.

**WP-3 · Render economy** *(P0 #1 sustained stutter)*
Cache `CapabilityContext` behind a generation counter **[the only CONFIRMED perf finding]**; leading-edge + ~50 ms trailing debounce in `RaiseChanged`, measured against **both** subscribers — the diagnosis missed `MainWindow.OnStateChanged` → `RefreshTrayMenu`, a Dispatcher post + WinForms tray update on *every* raise; hoist `GroupEvents` out of the per-race sweep; `ShouldRender` on TelemetryWorkspace / RaceTechnicalData / RaceReplayWorkspace / NavRail; guard the three unguarded interop initialisers at `TelemetryWorkspace:710-716`; hoist `AvailableRaceFilters` to a local. **Effort M, risk medium** — key `ShouldRender` on an explicit signature covering every field the markup reads.

**WP-4 · Navigation state model** *(P0 #1 structural re-analysis + half of P2.4 item 3)*
The MISSED items point here hardest. Split the single global `CurrentAnalysis`/`CurrentRaceCard` slot the two pages ping-pong; add `TuningWorkspaceOpen`/`CloseTuningWorkspace` mirroring the analysis pair; **delete the `TuningPage.OnAfterRenderAsync:472-481` auto-load** and the forced default at `:2981-2982` (a component-local guard destroyed on every navigation, so it re-arms every tab click and starts a full backend analyze); keep-alive or `@key` the `CompanionShell:26-37` switch so leaving Tuning stops disposing the whole analysis tree — today it re-runs `RebuildMapRenderCache` + `RebuildCalculatedTireWear` over every trace point and wipes all fourteen memo caches. **Effort M, risk medium.** Verify: navigating Tuning→Analysis→Tuning issues zero `analyze_iracing_race` calls when both caches are current.

**WP-5 · Watcher discipline** — narrow the recursive watcher to telemetry+setups, raise the 2 s debounce, drop extensionless acceptance, make the post-refresh sweep incremental, add interaction to the back-off. **Effort S, but risk MEDIUM-HIGH for its size** — this is the one perf finding the diagnosis itself rated medium confidence ("I could not observe the watcher firing"), and narrowing the filter risks missing new recordings. **Gate: validate against the existing discovery tests and confirm the real on-disk layout first.**

#### WAVE 2 — Correctness at the read boundary

**WP-6 · Sentinel normalisation layer** *(P0 #3)* — parallel-safe, touches nothing else in this wave.
One `LiveSdkSentinels` static (`UnknownLaps=32767`, `NotInWorld=-1`, `Unscored=0`, `UnlimitedSessionSeconds=604800`) with typed readers; route every read in `IRacingSdkTelemetrySource:106-168` and `:354-365` through it, deleting the ad-hoc inline comparisons that made the guards inconsistent field-by-field inside a single object initialiser. Fixes: 32767 laps-remaining (which also rescales that tile's sparkline to ~35,400 and flattens every real reading); `CarIdxF2Time` −1 fabricating plausible gaps; both-cars-at-−1 defeating the same-lap check; per-car lap/position/pace-flag/surface leaks in both producers. Mirror the constants in a shared Python module for `analysis_engine.py`/`race_foundations.py`/`storage.py`. Harden `RaceReplayWorkspace:635` so a position-0 car stops sorting above P1 in **already-archived** captures. **Land `SessionTimeRemain`/`SessionTimeRemainEx` (with its own 604800 guard) in the same change** — otherwise correctly nulling laps-remaining reads as a regression in exactly the timed sessions where 32767 appears.
Also fix the clamp at `:117` (`Math.Max(1, lapRaw+1)` publishes lap 1 for a not-in-world car and makes the catalog's "Lap not measured yet." unreachable). **The `+1` itself is an owner decision** — nothing documents it, no test covers it.
**Verify: a table-driven test feeding each named sentinel and asserting null/unavailable. That test is the deliverable, not the fix** — its absence is why 32767 shipped (`grep 32767 companion-app/tests` → nothing). Effort M, risk low (hot path — non-allocating statics).

**WP-7 · Lap timing** *(P0 #3)*
In `_lap_summaries`: prefer the already-loaded, entirely unused `LapLastLapTime` (first finite value in lap N+1, rejecting −1 and a repeat of the previously consumed value); emit `lap_time_source` + `lap_time_resolution_s`; keep the sampled fallback and fix its systematic one-sample undercount by ending at the first index of the *next* lap group. Extend `analyze-result-v1.schema.json`; surface the source in `RuntimeMapper:359/373`. Fix the sample-rate fallback disagreement (`or 20.0` at `:1005` vs `or 60.0` at `:7497`). **Do not touch the 20 Hz loader** — a documented deliberate memory decision.
**Effort M, risk MEDIUM-HIGH:** every derived pace number moves — fastest-lap selection, deltas, green-pace slope, run early/late averages, clean-lap statistics. **The two lap-time changes must land together**; the boundary fix alone shifts all times up ~0.05 s and would read as a real pace change.
**Two blocking unknowns, confirm before writing code:** (a) does editing `analysis_engine.py` rotate `pipeline_sha256`? If not, every analysed race keeps serving 0.050-quantised times after the fix. (b) The synthetic fixture has **no `LapLastLapTime`**, so it cannot regression-test the preferred path — regenerate the fixture or synthesise a `TelemetryTable`? *Owner call under the archive/fixture rules.*
**Better cross-check nobody used:** the IBT's embedded `SessionInfo.Sessions[].ResultsPositions[].LastTime`/`FastestTime` — iRacing's own full-precision official times, reachable today, zero repo hits. Use it to validate whichever channel the fix adopts and to serve older IBTs.

**WP-8 · Blank-scalar YAML → the `{}` defect** *(promoted from P3 to correctness)*
`ibt_reader:413,435` `{}` → `None`; harden `_as_text` to refuse containers; drop the `TrackDisplayShortName` fallback at `analysis_engine:827` (an empty dict is falsy, which is the *second*, different corruption — "Richmond Raceway Richmond"); coerce identity scalars at `:817-844`. Fix `race_foundations:712`, where a blank `CarClassShortName` falls through an `or` chain to the **numeric** `CarClassRelSpeed` and a class label becomes "60". Fix `race_card:186 _is_oval`, which reads the short name — **the None coercion alone does not fix it**; the correct source is `TrackType`, already read at `:828`.
**Render sites the diagnosis missed and any fix must cover:** `TuningPage:29`, `SetupPage:124`, `AnalysisPage:83,90`, `PlanningPage:127`.
**Migration hazard — owner decision (see §4):** changing the blank scalar flips discovery group ids from `…:{}:…` to `…:unknown:…`, and `workflow.py:527-540` matches archived analyses by that key — **already-archived analyses for config-less tracks are orphaned**. Nulling `track_config` changes persisted slugs and **violates `contracts/track-phase-visualization-v1.schema.json:36,39`, where `track_config` is required with `minLength: 1`.**
**Verify** *(this is the CONFIRMED prevention finding)*: every YAML fixture supplies a non-empty value for the field that breaks, and the fallback parser has **zero** direct unit coverage. Add a blank-scalar fixture variant; add a test asserting the fallback parser and `yaml.safe_load` produce identical output — PyYAML is optional and never vendored, so the buggy fallback is the parser that **always** runs and the two paths currently disagree; add a C# assertion that blank `track_config_name` yields `Layout == string.Empty`.
*File separately:* the real answer to the review's mojibake hypothesis is `ibt_reader:465-469` decoding as `utf-8-sig`/`cp1252` while the block declares `ISO_8859_1` (with a cp1252 scrub table in `race_card` applied nowhere else). And `(s)` is literal backend prose, not corruption — **owner confirmation needed** before touching evidence-labelled strings.

**WP-9 · Archive readability + envelope unlock — the gate on the highest-value item**
(a) `Strategy.to_payload` emits `None` (or drops infeasible plans and reports counts+reasons) instead of `math.inf`; `allow_nan=False` at both writers. **Every archived `analysis.json` for a race needing a fuel stop is currently unreadable** by `CompanionState:3430`'s strict parse → JsonException → cache miss → full re-analysis. This is simultaneously a P0 perf cause and a P1 blocker. (Today's test passes only because Python tolerates `Infinity`.)
(b) Add the three entries to `ANALYSIS_VIEW_FIELDS` and regenerate `analysis-view-v1.schema.json` via `tools/export_contracts.py` — never hand-edit.
(c) Fix `PlayerCarIdx`: it is loaded nowhere, so `field_pace.relative` is permanently `[]` and `position_context` permanently degenerate. Fall back to `DriverInfo.DriverCarIdx` from `table.session_info` — the same lookup already used twice in the tree.
**Do NOT add the three names to `RuntimeEnvelopeValidator.AnalysisViewFields`** — `RequireKind` throws on absence, which would make every previously cached envelope unreadable. Map defensively; absent object ⇒ unavailable.
Effort S–M, risk low; the bundle-hash rotation correctly self-heals. Verify: the two envelope contract tests go green automatically; add `json.dumps(payload, allow_nan=False)` over infeasible plans, and a `DriverCarIdx` fixture yielding non-empty `relative`.

**WP-10 · The one cache-schema bump.** 12 → 13, covering WP-6/7/8/9. **Depends on WP-1 being in.** Consider versioning only the projection layer so a stored response can be re-mapped rather than re-computed — otherwise every future bump costs the user a full library re-analysis with no migration path. XS mechanically; the design question is M.

#### WAVE 3 — Highest-value product work (parallel with primitives)

**WP-13 · Shared UI primitives** — *this is the inversion the brief asked me to name; it starts Wave 3, not P3.*
Add `MetricRow`, `MetricTable`, `SectionHeader`, `DisclosureSection` to the dossier's specs **literally**, including the deliberate omission of padding/tone/height/icon-colour parameters on `DisclosureSection` — those parameters are what allowed the divergence in the first place. Retire the four dead primitives and their dead CSS after migration.
**One correction to the spec:** Blazor omits a `bool`-valued attribute when false and emits it bare when true, so `aria-expanded="@Open"` would regress screen readers. Keep the explicit ternary-string pattern the existing bars already use.
Effort M to build, **L to migrate — do it screen by screen; the primitives can coexist with the one-offs.** Risk: many tests assert `coach.css` by substring. **Enumerate exactly which selectors are pinned before deleting any ruleset** — the dossier flags that enumeration as not yet done. Also grep `wwwroot/*.js` for the dead primitives' class names before deleting; the audit covered `.razor`/`.cs` only.

**WP-11 · Crew-chief Technical Data — XL. Three increments; do not attempt as one change.**

*11a — contracts and mapping (no visual change).* DTOs + mappers beside `AnalysisTechnicalInsight`; three nullable properties on `AnalysisWorkspace`. **Every scalar nullable** — the unavailable shapes *omit* keys rather than nulling them, and the three blocks omit different key sets. Handle the six real gates: absent-key vs present-key-with-NULL-reason unavailable; `tire_energy` unavailable while `time_loss` survives (shares null, ranking intact); segments excluded from `priorities` but present in `time_loss.segments`; **`strategy_planning` top-level unavailable with `pit_loss` still populated — gate the sub-blocks independently, never on the top-level status**; `plan_comparison` "limited" = one feasible plan, never "the winning strategy"; `field_pace` "limited" with empty `relative[]`. **`field_pace.cars[]` is sized to the full 64-slot CarIdx width, not the entrant count**, so status is "limited" in essentially every real race — filter to `status=='usable'` before counting or plotting, and do not treat "limited" as degraded data. Effort M.

*11b — backend narrative + evidence labels. Do this **before** the UI.* Two gaps the diagnosis surfaced: **no field in any of the three payloads carries an `evidence_type`**, so the proposed `--evidence-*` chips have nothing to bind to and synthesising one in Razor would be the UI inventing an evidence label; and **the three blocks have no consumer anywhere in the Python tree**, so unlike `technical_insights` there is no backend-authored sentence to reuse — a composed "diagnosis / significance / confidence / action" line in Razor is exactly the overclaim surface. Add `evidence_type` per headline figure and emit a `technical_insight`/`coaching_signal` from the producers. **Acceptable v1 alternative if cut for time: render `classification`, `limitations[]` and `excluded_from_model[]` verbatim in a disclosure and ship no composed narrative — say so explicitly rather than letting the UI fill the gap.** Effort M–L.

*11c — the workspace.* Three zones: A verdict rail (one sentence + three headline figures, replacing four "What to carry forward" bars); B ranked priority **table** — 12 rows replacing ~84 tile-values, with inline proportional bars carrying the recoverable-vs-energy join that is the whole point of `pace_attribution`, and excluded segments in one expandable footer row; C strategy (break-evens are the single most actionable output in all three payloads) + field dot strip gated on `measured_car_count ≥ 3` + the existing detail as a four-tab pane.
**Keep, do not rewrite:** the tire band model with right-side inner/middle/outer reversal, the lap-fraction pit timeline, the four-corner tire diagram, the early/late pace vector, the fuel scheduled-vs-range bar, `TechnicalUnavailable`'s Title/Detail/Action triad, and the existing evidence/tone plumbing.
**Delete:** the 2×2 quadrant, `MetricDensityClass` as a layout driver, `align-content:stretch` on metric containers, the centred two-row metric, the four foot bars, the four hardcoded accent hexes.
**Three claims it must never make**, each backed by producer text: the ranked total is a **ceiling, not a lap time**; the plan ranking is a **race-time comparison, not a finishing-position prediction**; tire energy is a **proxy, not wear**. `tire_energy_share` is a fraction (×100); segment min-speeds are **m/s**; `uniform_fallback` names are even distance slices S1..S12, not corners. Never join `car_index` to a driver roster — the key set is privacy-test-frozen to seven keys; label "Car idx N" or an anonymous rank. `field_median_lap_s` is pre-outlier-removal while each car's median is post — label it "field median, all recorded laps", never "field green pace" (competitors get no green/pit/incident screen).
Depends: WP-9, WP-10, WP-11a/b, WP-13. **Effort XL, risk high — the largest UI change in the plan.**

**WP-14 · Theme generation.** There is **no generator** — both "generated" files are hand-maintained, the only guard is a 25-entry colours-only parity test, and `theme.dark.json`'s entire `typography` block is never emitted, which is why `--metric-font` has 65 references and no definition. Add `tools/generate_theme.py`, wire into `verify_repository.py`, add a byte-identity test, then emit typography and fold the hand-invented tokens back. Three live rules hard-code the family and both hosts bypass `uiFamilies`. **Land the `--metric-font` definition separately** — it changes the rendered font on 65 rules at once.

#### WAVE 4 — Live telemetry

**WP-12 · Cheap fill fixes (S, do first).** Retarget `live-telemetry.css:79-108` to `.style-chart`. **Two things it does not cover:** gauge tiles have the identical defect and no `.style-gauge` rule exists anywhere — and `throttle` defaults to Gauge, so the out-of-box widget reproduces the complaint the moment the trend fix lands; and the non-chart half of the wasted space is separate — nine `cqi` occurrences, zero `cqb`/`cqh`, inside `align-content:center`, so a 380×300 card renders a 28–34px glyph in ~250px of nothing. **Fixing only the trend selector leaves two thirds of the screenshot unchanged.** Replace the vacuous guard test (it asserts the CSS *contains the text*, never that the class is emitted — which is why the regression shipped green) with a coupling assertion derived from `StyleClass`.

**WP-17 · Chart geometry: the immovability invariant (L).** The **CONFIRMED** root cause. Absolute-time ring (`bucket = floor(t/delta)` in epoch time, fixed `delta`); first/min/max/last per bucket **with their own timestamps** (the envelope currently draws at the bucket's *last* sample's timestamp, geometrically wrong even after the grid fix); latched Y range via the `stableRange` hysteresis already shipping in the sibling file; `lapSeconds` latched per stint and removed from `ConfigurationChanged()`; one `Path2D` per panel stroked under `translate(…)` from a slewed display clock, rAF alive while live; device-pixel canvas sizing with CSS write-back.
Same-file defects the diagnosis missed, all cheap once here: `clearPoints` resets `lapSeconds` to 60 → a ~2.7× horizontal rescale on epoch change; `append` has no monotonicity check while the window is located by binary search; `draw` discards `resize()`'s return value so every disclosure toggle rebuilds against a stale `bucketCount`; the null-run segment counter degenerates a nullable channel (steering *is* nullable) into disconnected moveTo's; 1.75px stroke at ~1px pitch overdraws into a band that amplifies every substitution.
**Prerequisite, not a nice-to-have:** neither chart file is behind the UMD wrapper — the pure index/mapping functions must be exported before the invariant is testable. **Gate hazard I verified: CI runs `node --check` only, over exactly nine files, and hard-throws if the count ≠ 9; `node --test` over `companion-app/tests/javascript` does not run in CI at all.** So adding a module requires bumping that number, and the regression guard is worthless until this package adds the test step. Apply the same absolute grid to the Trend tiles, or promoting traces into the grid inherits the fuzzing. **Risk high** — a ring-indexing bug drops or duplicates the newest column; land tests before draw code.

**WP-18 · Live telemetry unification (XL, three increments).**
**Safety prerequisites before any new widget id ships:** relax the whole-layout discard so an unrecognised MetricId drops the *tile*, not the user's entire named dashboard, and guard the unguarded `Get()` in the grid. The corruption archive is a **one-shot** while the loss is persisted on the same load — **the second rollback event permanently deletes dashboards with no recoverable copy** — and `BuiltInDashboardsInitialized` is a one-way latch, so the shipped Race/Qualifying dashboards are never re-seeded either. Display styles and trend durations persist as **integer ordinals** (no string-enum converter anywhere): append after `Status`/`ThreeLaps` only, and add a test pinning each ordinal.
*18a* registry seam — a `WidgetKind` field plus a signal list on the tile (preferred over polluting every metric's `Styles`); `ExtensionData` makes both downgrade-safe. **Register composites completely** — an empty `Units` list or a `Styles` list omitting its own style makes `ValidateAndRepair` rewrite every user instance on next load. Give composites their own preview path.
*18b* track map + lap trend lift almost verbatim; "current telemetry values" is a saved 6-tile arrangement, not code.
*18c* the only genuinely new renderer: multi-signal stacked chart (one engine freezes four panels, the other is strictly one series per canvas). **Do not instantiate `LiveTelemetryVisuals` per tile** — contribute to the existing single batched `append`.
Migration orphans exactly one setting, `HistoryLaps` (1–10), with no tile-model equivalent; extend the duration vocabulary or add `TrendLapCount`, seed from the existing value, and **do not delete `HistoryLaps` this release**. Five structural tests pin the file's existence and exact markup and must be rewritten in the same change — preserve the *intent* of the off-screen render-suspension assertion.

#### WAVE 5 — Information architecture

**WP-15 · Setups: one setup end to end** *(highest-priority acceptance criterion)*
Critical path, in order: (1) season becomes a **ranking**, not a filter — two-tier selection carrying `season_match` and a **new** status value, season-matched first (ST-003); (2) role becomes a preference inside `_preferred` — never accept a differing explicit role (ST-001); (3) add `message` + `blockers[]` to every non-ok return from the already-computed candidate counts — `Text(response,"message")` is always null today, so the hard-coded sentence always wins, which *is* the "unexplained dead end"; (4) send the raw car folder id, not the prettified display name (`porsche992rgt3` → "Porsche 911 GT3 R (992)" matches nothing; it works for the Supra only because the query is hard-coded), carried hidden per SU-009.
**Five things the diagnosis missed that the critical path must account for:** track is a **third** filename-token filter — fixing season and role still leaves it; **`Purpose` never reaches the backend** at all, so the Race/Qualifying toggle cannot change which candidates are searched; the **confirmed** status is unreachable for every car but the Supra, so a user who does everything right still sees a hedged "provisional" result; **every keystroke invalidates an in-flight request** with a message that names no cause; and **step-1 defaults are computed once, before the inventory exists**, so the prefill is blank and never retried while the primary button's enabled state depends on refresh timing. Also: catalog truncation is silent, and html-only setups are invisible to the app but rankable by the backend.
Off critical path but required for the review's other bullets: replace the `<input list>` datalists (no dropdown affordance at all in WebView2; `@bind:event="oninput"` rewrites the value and dismisses the popup every keystroke) with the `<select>` pattern already used elsewhere; publish inventory as immutable arrays assigned atomically instead of `List<T>` a background thread clears mid-render; compute Available/Incomplete/Unavailable/Incompatible from `State.Setups` with a thin predicate so the backend stays authoritative.
**Do not put the donor path on the critical path** — it is called without `track_characteristics`, scores every family at 0, and there is no deterministic local source of banking/corner-duty data. Surface it as a blocker.
Effort M for items 1–4, **L including dropdowns and badges.** Verify: **a Python test whose stems carry no season token and no role token** — every current fixture bakes the shop convention in, which is why no test covers the ordinary case.

**WP-16 · Lap rail, run header, footer** *(P2.1)* — six competing templates across two files, **five dead and the surviving one is the broken one**; the narrow-mode layout can never apply; there is a `gap-inline` typo that is not a CSS property. One token-driven template with exactly ONE flexible track, applied to rows **and** `<summary>` with identical `padding-inline`; four equal footer slots; delete the five dead templates and the 3-track-era fossils. Two drift sources the diagnosis missed: the fuel column collapses ~55px→~10px when a lap has no recorded fuel, and the identity column widens past lap 100. **Row *height* is also unstable** — the delta `<small>` renders only on non-fastest rows, so a column-only fix leaves the rhythm ragged; reserve the slot. Optional cells are already reserved in markup and thrown away by CSS; the one genuine markup gap is the flag cluster, which needs ordered semantic slots. Effort S–M; three tests pin the current strings. **Needs-confirmation:** the 72px minimum is arithmetic, not a measured render — verify at the 900px window minimum and non-100% display scale.

**WP-19 · Home row** *(P2.2)* — **the dominant cause is not the eligibility inconsistency the diagnosis leads with.** `DashboardMapper` constructs `RaceOverview` with **six values out of seventeen**; every row it serves renders a dash with no eligibility rule and no calculation involved. That explains both symptoms — and why the gap is row-dependent, not race-dependent. The upgrade path is best-effort and silent: only six races are enriched, enrichment returns untouched on any cache rejection, and the merge replaces `Overview` wholesale, so one stale stamp sends a fully analysed race back to all dashes while still showing "Analyzed".
Then collapse the two clean-lap rules **toward the stricter one** (the faithful port of the backend's) — not the looser; that would silently widen an evidence rule the backend owns. This makes some populated values disappear, which is correct but reads as a regression unless the four explanatory states ship in the same change: Not analysed / No eligible evidence (with the dominant reason) / Not computable ("needs 3 green laps") / Unavailable reading. Pure label plumbing — every string is backend-authored. `HomePage` never reads `Analyzed` today, and the vocabulary that exists is attached to an unreachable nested `title`.
Make the rightmost column monomorphic — today it changes with **strategy, not with the driver** (a zero-stop race can never show "Tire left"), and `ControlLoadChangePercent` discards its sign at both writers so "18% change" is uninterpretable. Recommend **Fuel used** (already emitted, mapped, and consumed by Race Planning — keep the "Not recorded" guard, since an absent channel sums to `0.0` not `None`); zero-risk alternative **Stints**. Stop hiding the Pace column below 1180px. Effort M; use named arguments across the three construction sites.

**WP-20 · Planning briefing + Tuning parity** *(P2.3/P2.4)*
**The real blocker the diagnosis missed:** `PlanningInstructionIsActionable` rejects any backend claim whose first token is not one of ~20 hardcoded verbs, then substitutes the mapper's own Inferred text — **this makes every "derive it in `race_card.py`" proposal inert until it changes.** Fix it first. `PlanningPaceClaim` also returns a hardcoded string for short races *before* that check runs, and `PlanningTriggers` drops the `_fuel_response_claim` the backend already emits.
De-alias `TireGuidance` from `paceClaim.Text` — the two columns render the identical string, which is the literal duplication in the screenshot; removing the trigger fallback alone will not blank the column. Delete the `Balance response` and tire fallbacks (else-branches of a repair check, not measurements — **every string visible in the reviewed screenshot is a fallback**), paired with a one-line "why this is empty" note.
**The specific reason that screenshot's bottom half is empty is a distance mismatch:** the decision is marked inapplicable when requested laps differ from `ScheduledLaps` by >1e-9, and the screenshot shows 55 against "57 laps scheduled" — a ±1-lap typo deletes every measured line, with no visible "use scheduled distance" control. Add one.
Measured replacements are reachable **today with zero contract change** via `analysis_view.runs`/`strategy` — and `race_card` is outside the analysis_view contract entirely, so adding derived claims there needs **no schema work**. Reuse the existing numeric-target gate; keep the repair screen in front of every number; **raise the `_compact` budgets** (12 words/82 chars) or the new copy arrives truncated.
Headline overlap: `line-height: 20px` is a **length**, inherited as computed 20px into a 30px clamped headline. Minimum fix is one declaration. **Do not bundle** the global unitless conversion — it reflows every screen. Second half: the stacking breakpoint is a **viewport** query at 1050px while the parent stays two-column to 1320px, so the card is narrow and the query inactive across ~1050–1600px — convert to a container query (already used in this codebase).
Tuning parity: extract the race list into a shared `RaceBrowser` with a `Groups` override (the eligibility pools genuinely differ), search and filter bound to shared state; context bar + back button. Workspace flag and auto-load deletion come from WP-4. Tests pinning removed literals exist on **both** sides (C# and Python).

**WP-21 · Race Analysis trace presentation** — **the claimed cause does not execute.** `RepresentativeIndices` is dead code for lap traces (~160 bins vs a ~1100 budget → the early return always fires). **The actual cause is the backend reducer:** each of the 160 distance bins is reduced with `signed_peak()` for exactly the channels that look jagged, while speed/throttle/gear/rpm use `_median()` — a signed peak alternates sign between adjacent 25 m bins, so the polyline is a **maximal zigzag by construction**. Confirm by observing the median-reduced signals are smooth in the same chart. Fix in the presentation choice (centre line + `steering_abs_peak_rad` envelope band, channels already shipped) or in the reducer — **not in a new UI decimator.** File the REFUTED viewBox item separately as its own layout-fidelity defect.

**WP-22 · Settings density + popout chrome** *(P3.1)* — the two Data boxes carry ~1 bit (same expression twice, plus a static literal), the pill overclaims across three folders while testing one, and the same three paths appear a **third** time in Troubleshooting — so the fix must decide which surface owns paths. `align-items: start` is the actual fix for the vertical waste; compacting rows alone will not remove it. Disclosure convergence: exactly four differences remain; **why five passes failed** — the guard test asserts three substrings and stays green, `.diagnostics-section` has five declaration sites across two stylesheets, and a complete fossil of an earlier attempt is still in the tree with **no `.razor` reference**, so a pass that "fixed the disclosure bar" there changed nothing on screen. **Even a byte-identical bar will not match**: one body is two 300px cards, the other is 27px rows — the component needs a body contract. Popout glyphs are **geometrically clipped, not badly drawn** (16×16 content box, 19×19 paths; focus raises the border and clips 2px more — the state in the screenshot; the 15px Close is uncut, which is why the X alone looks right); fix with caption metrics, a fixed Viewbox, no focus-driven border change, no `Stretch="Uniform"`, and separate caption controls from app commands. The close button also never reaches the window edge.

**WP-23 · Garage61 — small code items only; the rest is owner action.**
A **429 is currently reported to the user as a corrupt response** — only 401/403 are special-cased, so a throttle becomes "could not be trusted", telling the user to re-check a credential that is fine, while the correct `throttled` outcome, message and `retry_later` classification **already exist and are unreachable**. Add `Garage61ThrottleError`, honour `Retry-After`, map it. More broadly there is **no rate-limit posture at all** despite `G61-006` requiring one, while a single sync issues two searches at `limit` 1000 plus up to eight CSV downloads. **The repo never calls `GET /teams`**, so scope is stamped "own/team" without knowing whether the user is on any team — and the zero-team degenerate case, where the "external reference" is the driver himself, is undetected and unlabelled. The client is **stricter than the API** (requires `tracks`; the API accepts track OR car OR user) with no diagnostic separating "no laps exist" from "our filters removed them all", and three documented pool-shaping filters are unused — `extraDrivers` in particular is the platform's own sanctioned way to include specific consenting drivers. `global_visible_laps_approved: true` is an **unverifiable self-declaration** that changes only the label, never a request — do not set it speculatively. Fix the overclaiming wording: the approved pool is "laps whose owners made them visible to this user", never "all community laps". Read and report the full `apiPermissions` list instead of one flag.
**Scraping is settled as a non-option on three independent grounds** — `robots.txt` disallows `/app/` where every lap page lives, no lap data is reachable unauthenticated so there is no public leaderboard to read, and `G61-006` plus the product non-goals forbid it in writing. Do not build it and do not design around it.

---

### 3. Parallelism — what parallel agents can safely run in one worktree

**Single-writer files. Only one in-flight package may hold each; the order is the schedule:**

| File | Serial order |
|---|---|
| `CompanionState.cs` | WP-1 → WP-3 → WP-4 → WP-5 → WP-15 → WP-19 |
| `RuntimeMapper.cs` | WP-11a → WP-15 → WP-19 → WP-20 |
| `analysis_engine.py` | WP-7 → WP-8 → WP-9 → WP-11b → WP-21 |
| `coach.css` | WP-13 → WP-12 → WP-16 → WP-19 → WP-22 |
| `TelemetryWorkspace.razor` | WP-3 → WP-16 → WP-21 |

**Genuinely parallel lanes — no shared file with any other lane:**

- **Lane S** WP-6 — `LiveTruthPolicy.cs`, `IRacingSdkTelemetrySource.cs`, `RaceReplayWorkspace.razor`, `race_foundations.py`, `storage.py`
- **Lane M** WP-2 — `McpBackendClient.cs`, `start-mcp.ps1`, `mcp_server.py`
- **Lane J** WP-17 — `live-telemetry-chart.js`, `LiveTelemetryVisuals.razor`, `ci.yml`, new JS tests
- **Lane W** WP-18a/b + safety — `LiveMonitorLayouts.cs`, `LiveTelemetryCatalog.cs`, `SettingsStore.cs`, `LiveTelemetryLayoutGrid.razor`, `LiveMonitorWindow.*`
- **Lane G** WP-23 — `garage61_client.py` (its one mapper line batches into whichever `RuntimeMapper` slot is open)
- **Lane T** WP-15 Python half — `tuning_workflow.py`, `setup_catalog.py` (its C# half waits for the coordinator slot)
- **Lane R** WP-16's `analysis-performance.css` (sole owner) can start immediately; only its `coach.css` deletions queue

**Contested test files needing an explicit merge discipline:** `LiveMonitorTests.cs`, `CapabilityRegistryTests.cs`, `IterationUxTests.cs`, `RaceAnalysisBehaviorTests.cs`.

---

### 4. Cannot proceed without the owner

1. **The sanitized tire aggregate.** *Correction to the brief:* no `blocked_awaiting_aggregate` gate exists in any of the three payloads — it lives in `tire_model_gate.py`, a module with **no caller in the analysis engine**, referenced only by its own test. The UI has no gated field to handle. The real decision is upstream: do you want the gate wired at all, and will you supply the aggregate? Until then `tire_energy` stays an explicitly-labelled **proxy, not wear**, and the UI must keep saying so.
2. **One authorized Garage61 check.** Highest-value question: **can a data-pack subscriber read content under a team they are not a member of?** Evidence cuts both ways and it is answerable with two read-only calls on official endpoints using the stored token — agents were forbidden authenticated access. Bundle three more into the same session: `GET /teams` (are you on any team at all?), the full `apiPermissions` list, and `GET /me/statistics`.
3. **Garage61 permission decisions.** Ask Garage61 to **approve this application for visible-lap search** — the sanctioned remedy, quoted verbatim in their OpenAPI, with the entire switch already built end-to-end and defaulted off; approval is the only missing input (the default app is *narrower* than the website by design, and no scope value widens it). Then: join or create a team (alone satisfies "representative comparison lap" with zero code change, at the cost of a small self-selected pool); pursue data packs, including any paid-pack purchase; and decide whether the in-policy manual-CSV export path is sanctioned — if so its provenance gap must close, because a hand-dropped CSV currently enters the same door as an API lap with no lap identity, owner, retrieval state or content hash, which `G61-008` requires.
4. **G757 hardware validation.** **No area of the dossier covers it** — eleven diagnostic passes returned zero findings referencing it. It requires your physical rig and a live iRacing session, which no agent can perform read-only. Tell us what it is and what "validated" means and it can be scoped; until then it is unplanned, not deferred.
5. **Migration decisions that change persisted keys.** (a) `track_configuration_key` — the layout fix **orphans already-archived analyses** for config-less tracks, changes persisted slugs, and **violates a contract where `track_config` is required with `minLength: 1`**: migrate or accept re-derivation? (b) `None` or `""` for a missing configuration? (c) should the cache version become a *projection-layer* version so a stored response can be re-mapped rather than re-computed?
6. **Define "create at least one setup end to end."** ST-005 forbids producing a loadable `.sto` and the contract already returns `simulator_loadable_setup_produced: False`, so the only coherent meaning is "reach step 4 with a saved package and rollback fingerprint" — confirm, and confirm step 1 should say so, or users who succeed will believe they failed.
7. **Two answers only the reviewer has.** Was the ~20 s the Race Analysis tab click alone, or tab click plus opening a race? (It changes which P0 package dominates.) And is proper pluralization what you want, given it touches evidence-labelled prose and the tests asserting it?
8. **Two one-command checks on your machine.** One `ls` of your iRacing setups root (settles whether `baseline_confirmation.confirmed` can *ever* be true). One Richmond `.ibt` — the `{}` mechanism is proven against real data, but no Richmond recording exists locally and every local track writes `TrackConfigName: Oval`, which is why none of them show the glyph.
9. **Product calls.** WPF popout parity for new rich widgets vs degrading to a bare number; a real minimize on the popout; whether density tokens belong in `theme.dark.json`; whether the shared race list greys ineligible races or filters them; whether planning sentences may be composed in `RuntimeMapper` or must be authored in `race_card.py`; and the `Math.Max(1, lapRaw + 1)` off-by-one, which nothing documents and no test covers.

---

### 5. Gates, and two that will bite

Per package: `dotnet test companion-app/iRacingCoach.sln`; `tools/verify-repository.ps1` (carries contracts, fixtures, backend suite); `tools/export_contracts.py` regeneration for any schema change — never hand-edit `analysis-view-v1.schema.json`.

**Environment.** On this machine the only Python is a Windows Store app-execution alias, so `tools/dev/Invoke-SandboxedDev.ps1` aborts on an unhashable reparse point and reports exactly `FAILED (failures=8, skipped=1)` **regardless of the change under test**. Always bind the interpreter explicitly — `-PythonPath` or `$env:IRACING_COACH_PYTHON` — and the gate passes clean (182 tests: 181 passed, 1 skipped).

**CI, verified.** The JavaScript gate is `node --check` only and **hard-throws if the first-party file count is not exactly 9** — any package adding a `.js` file must bump that number in `ci.yml`. And **`node --test` over `companion-app/tests/javascript` does not run in CI at all**, answering the dossier's open question: the immovability regression guard is worthless until WP-17 adds the step.

---

### 6. Honest sizing

**XL — each is a redesign, not a refactor:** WP-11c (crew-chief workspace), WP-18 (live telemetry unification, three increments).
**L:** WP-2 (persistent MCP session), WP-17 (chart geometry), WP-13 (primitive migration), WP-15 (setups in full).
Everything else is M or smaller. Roughly two XL, four L, a dozen M.

Do not let WP-11c or WP-18 be folded into a "polish" milestone. And do not build WP-2 before WP-0.2 reports numbers — it is the largest change in the P0 set, and if the backend analysis run dominates, the persistent session buys ~0.5 s against a multi-second cost.