# Gaps, Contradictions, and Open Questions

This is the current criticism backlog. Items remain open until evidence closes them; they are not hidden in optimistic release prose.

| ID | Issue | Why it matters | Closure evidence |
| --- | --- | --- | --- |
| `GAP-001` | Representative real-telemetry breadth remains incomplete. | Fixtures cannot prove every SDK field combination, timing, reconnect, car, or session variance. | Direct real-system evidence tied to exact builds and representative sessions, with failures retained rather than generalized away. |
| `GAP-002` | Closed in 0.11.0: the unused Setup Library surface was removed; internal setup indexing, recorded Track View, and Starting Tune remain aligned with capability truth and tests. | Capability truth must agree with user-visible reality and automated gating. | Reopen only with a named source/registry mismatch. |
| `GAP-003` | Closed in development in 0.11.0; packaged and real baseline acceptance remain quality gates rather than missing product behavior. | Starting Tune is first-class inside Setups. | Event/source/checks/run UI, package mapper, Q/R separation, and product-truth tests. |
| `GAP-004` | Machine-bound Garage61 credential policy is decided; implementation evidence is incomplete for the new migration/redaction/permissions matrix. | The destination PC must restore nonsecret state while requiring reconnection and never carrying a portable secret. | Migration, legacy-secret removal, redaction, and least-privilege storage tests tied to the packaged build. |
| `GAP-005` | Full grade-rubric calibration is not established against diverse real sessions. | Visually complete grades may imply precision beyond evidence. | Published rubric corpus, unavailable-category rules, expert review, and calibrated real-session results. |
| `GAP-006` | External Garage61 behavior is provider/account dependent. | Passing adapters do not prove current production endpoints or permissions. | Authorized integration acceptance with redacted request/response metadata and error cases. |
| `GAP-007` | Accessibility coverage is not exhaustive. | Keyboard, screen reader, scaling, contrast, and chart semantics can regress outside screenshot tests. | WCAG-oriented audit on packaged build with logged remediation. |
| `GAP-008` | Performance thresholds need broader representative libraries, real SDK sessions, display modes, and hardware. Current source separates SDK capture from display-synchronized full-page canvas/native-popout painting, removes the Race cursor's per-frame .NET callback, and gates background analysis behind live/interactive work, but no combined-load, 60 Hz, or 244 Hz acceptance claim exists. | Small fixtures may hide import, chart, list, interop, scheduler, and sustained rendering defects; `requestAnimationFrame`, a display dispatcher, or a source priority gate alone does not prove delivered frame rate or absence of contention. | Versioned benchmark corpus, exact executable/hardware/display context, real source cadence, delivered-frame and dropped-frame measurements, background-plus-live contention runs, thresholds, and percentiles. |
| `GAP-009` | Requirement authority is distributed across several handoffs and corrective prompts. | Agents can implement obsolete prose or miss a superseding constraint. | Consolidated approved product specification and archived/superseded source labels. |
| `GAP-010` | Closed as stale/unreproducible on 2026-08-03. Current authoritative Markdown, JSON, C#, Razor, and XAML contain no mojibake marker sequences under a repository-wide scan. | Preserving a third-party rendering/export defect as a source gap would misstate repository reality. | Reopen only with an exact current file, byte sequence, and semantic impact. |
| `GAP-011` | The repository includes generated Codex protocol schemas and preview dependencies of uncertain long-term necessity. | Vendor/generated bulk complicates review, updates, and security inventory. | Dependency decision documenting generation source, update cadence, license, and whether files should be fetched at build. |
| `GAP-012` | Support for all owned/downloaded cars depends on available local/recorded metadata. | The planning selector must not invent ownership or present stale/non-owned cars as fact. | Defined local source hierarchy, freshness/error behavior, and real account/library acceptance. |
| `GAP-013` | Closed for current derived vehicle sideslip: the deterministic engine computes signed degrees as `atan2(VelocityY, VelocityX)` only for paired finite samples with positive/usable forward velocity and at least 5 m/s planar speed; failed guards remain gaps, and Yaw Rate remains independent. | Yaw rate, vehicle sideslip, and tire slip are physically different; silently copying or relabeling one as another would create false evidence. | Python derivation tests passed 19/19, the coordinator mapper check passed 1/1, and one recorded Iowa IBT produced 542 valid samples and 9 guarded gaps across four laps. Reopen for a provenance/guard/mapping defect. Broader cross-car/session validation remains `GAP-001`; this is not a native `SlipAngle` or tire-slip channel claim. |
| `GAP-014` | Closed in current source: live tile seed and append retain positioned missing samples as gap markers, range calculation ignores their null values, and path construction splits at each marker or material timestamp gap. | Truthful missing-data handling requires both correct values and correct continuity; a connecting segment would imply that an unobserved interval was measured. | Source assertions cover nullable seed serialization, missing-value range exclusion, segment breaks, and the absence of a `HasValue` seed filter. Reopen only with a reproduced continuity defect. |
| `GAP-015` | Closed in current source: the Home analysis scheduler checks a shared priority gate before each queued item, waits while live telemetry is connected or interactive analysis is active, and resumes sequential catch-up after foreground work releases the gate. | A long historical-library catch-up must not intentionally compete for the same analysis path as the user's live or explicitly requested task. | Source and focused tests cover both live and interactive gate states. Reopen for a reproduced priority bypass; representative combined-load throughput and contention measurement remains under `GAP-008`. |
| `GAP-016` | Current native iRacing incident evidence is based on observable cumulative incident-counter changes and does not identify every zero-point contact or classify the cause as car contact, wall contact, off track, or loss of control. The rail shows `x0` only for an explicit zero-point source event; an eventless lap remains blank. | Inventing an event type, adding placeholder/source prose in the driving UI, synthesizing x0 from an unchanged counter, or treating the counter as exhaustive would overstate the recording. | Close only when a supported source supplies explicit event records with lap/time/type provenance, plus representative real-session evidence. Until then type is omitted and the technical limitation remains here rather than cluttering the incident popover. |
| `GAP-017` | Exact-configuration vector-map extraction and pit-layer breadth are not yet validated across representative oval/road layouts, alternate configurations, reversed layouts, and recordings without usable geometry. | Borrowing a visually similar layout would put start/finish, pit entry/exit, commitment, merge, and replay cars in the wrong place. | Schema/key/hash validation tests plus representative real-layout visual overlays; every missing layer must exercise its reason/action path without cross-config fallback. |
| `GAP-018` | Race replay participant coverage depends on the car-array fields and sampling actually present in each IBT. A 2 Hz field stream cannot establish competitor controls, tire state, fuel, setup, incidents, or exact between-sample motion. | A visually complete replay can easily overstate what was recorded and make inferred competitor behavior look factual. | Coverage profiles and real-session evidence across field sizes/classes/flags/pits/disconnects, with gaps retained and every prohibited competitor field absent. |
| `GAP-019` | The NASCAR-first tire/capability model now enforces a deterministic three-matching-session floor and feature-aware confidence gates, but those thresholds are not calibrated against an error corpus and authorized Garage61 representative-lap acceptance is still absent. | A conservative implementation rule prevents immediate fabrication but does not establish that confidence labels or intervals are statistically calibrated. | Versioned training/holdout corpus, deterministic model tests, calibration/error report by car/track/setup/conditions, expert review, and redacted authorized Garage61 scope/alignment evidence. |
| `GAP-020` | Permanent raw-IBT retention intentionally increases portable storage and backup size; representative copy interruption, disk-full, duplicate, migration, and uninstall behavior are not yet evidenced. | Silent copy failure would make replay/reanalysis fragile, while silent pruning would violate the durability decision and copying raw personal files into diagnostics/source would violate privacy. | Atomic/deduplicated copy tests plus exact-executable storage, original-removal, backup/migration, disk-full/retry, uninstall, and privacy-scan evidence. |
| `GAP-021` | The new Race Analysis surfaces have not passed the exhaustive 1280x720/1920x1080 maximized/nonmaximized changed-control and intermediate-motion matrix. | Endpoint screenshots and source assertions miss clipping, jitter, stale transitions, overflow, inaccessible controls, and interaction regressions. | Complete `race-analysis-overhaul-acceptance-matrix.md` evidence for the exact executable, including reduced motion and supported/missing/error states. |
| `GAP-022` | Progressive Tuning evidence-contract v2 is implemented in development source, but its complete browser/native visual matrix and a real clean compatible O'Reilly/Xfinity open-setup A/B cycle have not been accepted. Official turn-map coverage is intentionally exact-configuration and sparse; other car families and exact numeric garage steps/ranges remain unavailable until versioned sources exist. | Source contracts and synthetic/focused tests can prove bounded identities and deterministic behavior, but they cannot prove that the dense map-first editor fits, moves, persists, or produces a useful real-world controlled test in the exact executable. Treating a synthetic result as real acceptance would also overstate setup causality and evidence breadth. | Execute every state in `progressive-tuning-overhaul-acceptance-matrix.md` at both required resolutions plus native restored/maximized/scaled states, then complete one matched real open-setup A/B cycle while preserving the manual-change, one-system, rollback, and non-causal outcome boundaries. |

## Open product questions

1. What exact Windows versions, WebView2 versions, scaling factors, and minimum hardware define the support matrix?
2. External-service credentials are explicitly excluded from portable migration and require reconnection on the destination PC.
3. What source is authoritative for owned cars when iRacing does not expose ownership locally in a reliable supported form?
4. What threshold of real-session coverage is required for each telemetry-related release claim?
5. Starting Tune remains within Setups; Progressive Tuning remains event-linked follow-up unless user research justifies a navigation change.
6. Which optional AI behaviors, if any, are release requirements rather than roadmap ideas?
7. Raw finalized IBTs are retained until an explicit durable-data deletion action; retention periods for histories, logs, derived caches, and backups remain open.

## Resolution rule

Closing an item requires a specification update, implementation change when applicable, and evidence. A comment such as "works locally" or a screenshot without scenario provenance is insufficient.
