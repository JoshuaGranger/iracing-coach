# Traceability Matrix

This matrix maps specification areas to implementation and verification anchors. A path demonstrates where evidence should be inspected; it does not by itself prove conformance.

| Specification | Main implementation anchors | Test/evidence anchors | Snapshot status |
| --- | --- | --- | --- |
| `HOME-*`, `NAV-*` | `CompanionShell.razor`, `NavRail.razor`, `HomePage.razor`, `CompanionState.QueueMissingHomeRaceAnalysis`, `WaitForHomeAnalysisWindowAsync` | `CoordinatorTests.HomeRefresh_PausesBackgroundAnalysisWhileLiveTelemetryIsConnected`, `HomeRefresh_PausesBackgroundAnalysisWhileInteractiveAnalysisIsLoading`, `FinalProductFixtureTests.cs`, fresh-development-executable walkthrough | Implemented; background queue and foreground priority gate are source/focused-test verified, private-library breadth and combined-load measurement pending |
| `RA-001` to `RA-025` | `AnalysisPage.razor`, `AnalysisWorkspacePage.razor`, `CompanionState.cs`, `DashboardMapper.cs`, `native_events.py` | `RaceAnalysisBehaviorTests.cs`, `FinalProductFixtureTests.cs`, `test_native_events.py` | Implemented; real-library breadth pending |
| `RA-030` to `RA-059` | `TelemetryWorkspace.razor`, `AnalysisTraceLayouts.cs`, `analysis-telemetry-cursor.js`, `analysis_engine.py` | `RaceAnalysisBehaviorTests.cs`, `CapabilityRegistryTests.cs`, 19/19 sideslip Python checks, 1/1 mapper check, scoped Iowa IBT evidence, full-telemetry tests, native fixture walkthrough | Configurable rows, any-two-distinct signal pairing with independent scales, draggable signal/drop model, cool-to-warm speed map, tooltip-capacity cursor pooling, independent Yaw, and guarded derived vehicle sideslip implemented; broad channel breadth and measured latency required |
| `RA-060` to `RA-112` | integrated analysis insight rail and lap/run popovers, `analysis_engine.py`, `reporting.py`, `race_card.py` | analysis/report/damage/race-card tests and native fixture walkthrough | Broad implementation; some rubric/data combinations partial |
| `PLAN-*` | `PlanningPage.razor`, planning contracts/mappers, deterministic report data | `FinalProductFixtureTests.cs`, reporting/workflow tests | Implemented fixture workflow |
| `SETUP-*` | `SetupPage.razor`, `RuntimeMapper.SetupPackage`, `setup_catalog.py`, coordinator contracts | setup-catalog, package, and product-truth tests | Internal indexing and Starting Tune implemented; unused library UI removed |
| `TUNE-*` | `SetupPage.razor`, `TuningPage.razor`, `TuningTrackSelector.razor`, `tuning_engine.py`, `tuning_workflow.py` | tuning-engine/workflow tests | Starting Tune and event-linked progressive tuning implemented; real A/B pending |
| `LIVE-*`, `LMG-*` | `LiveTelemetryLayoutGrid.razor`, `live-telemetry-layout.js`, `live-telemetry-tile-charts.js`, `LiveMonitorWindow.*` including `CompositionTarget.Rendering`, `LiveMonitorLayouts.cs`, `LiveTelemetryCatalog.cs`, `LiveTelemetry.cs`, `IRacingSdkTelemetrySource.cs` | `LiveMonitorTests.cs`, replay/live service tests, fresh-development-executable popout walkthrough | Equal-share full/popout layouts, bounded timestamped full-page renderer, missing-sample gaps, and incremental display-synchronized native tile updates implemented; no real-SDK, 60 Hz, or 244 Hz acceptance claim |
| `CONN-*`, `SET-*`, `DIAG-*` | embedded `ConnectionsPage.razor`, compact `SettingsPage.razor`, `DiagnosticsPage.razor`, stores, logging | coordinator/backend and production-source tests; integrated visual review pending | Implemented inside compact Settings hierarchy; real external auth varies |
| `EV-*` | Python analysis/report/tuning modules, `EvidenceBadge.razor`, `RuntimeMapper`, grade audit UI | contract, engine, and product-truth tests | Five-category availability is explicit; rubric calibration still requires representative expert review |
| `TD-*` | IBT reader, native events, storage, analysis engine, live source | Python telemetry/event/storage tests, derived-sideslip tests, coordinator mapper test, scoped Iowa IBT probe | Broad implementation; derived vehicle sideslip is validated for the named recording but is not a native `SlipAngle` or representative all-car acceptance claim |
| `PORT-*`, `SEC-*` | durable archive, portable logical settings, machine placement/credential stores | coordinator, monitor migration, and path-security tests, privacy scan | Machine-bound credential and placement split implemented |
| `ARCH-*`, `PROC-*` | app startup, coach engine, backend client, coordinator | backend/coordinator tests and release build | Implemented local runtime shape |
| `G61-*` | Garage61 client and credential UI/store | Garage61 adapter tests | Adapter/configuration implemented; real service acceptance conditional |
| `AI-*` | coach-engine orchestration and MCP contracts | engine probe, contract tests | Deterministic engine implemented; optional AI is noncritical/partial |
| `INST-*`, `UPG-*`, `UN-*` | installer/uninstaller, release/lifecycle scripts | installer-upgrade evidence | Locally verified; representative-PC breadth pending |
| `QA-*`, `REL-*` | build/verify scripts and release packet | release records, targeted iteration evidence, direct local/native validation | Gate scope follows the changed behavior; packaged releases require full applicable release evidence |
| `UI-*`, `ACC-*`, `PERF-*` | `coach.css`, Razor/WPF surfaces, full-page canvas renderer, native retained-tile renderer, `analysis-telemetry-cursor.js`, Home analysis priority gate, performance probe | visual baselines, UI audit, targeted JavaScript/.NET assertions, scheduler tests, performance packet | Current palette, incremental native rendering, tooltip-capacity-bounded DOM-owned Race cursor, and background priority gate implemented; exhaustive accessibility, combined-load, and real high-refresh acceptance pending |

## Maintenance rule

When a requirement changes, the author must inspect all mapped implementation and evidence anchors. When implementation changes behavior, the author must update the affected requirement or record a deliberate deviation. When a test changes without a requirement change, reviewers must confirm that the test still measures the requirement rather than merely current behavior.
